import React from 'react'

const PreviousButton = (props) => {
  return <button className='button-circle' onClick={props.previousIndex}>
    previous skincare!
          </button>
}

export default PreviousButton